// Component Styles
const styles = (theme) => ({
  root: {
    paddingLeft: theme.spacing(3),
    paddingRight: theme.spacing(3),
    display: 'flex',
    alignItems: 'center',
    flexDirection: 'column',
    maxWidth: 1200,
    marginLeft: 'auto',
    marginRight: 'auto',
  },
});

export default styles;
